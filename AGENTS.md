# AGENTS.md

Universal compatibility file for AI coding agents.

The canonical instructions for this repository live in **`CLAUDE.md`**.
Follow the instructions there.

The house-style rules are skills in the `hexagram` plugin, installed per machine rather than
copied into this tree — see section 12 of `CLAUDE.md`. The pre-plugin copies that used to sit in
`.claude/rules/` and `.claude/skills/` have been deleted; section 13 says why, and notes the one
mirror still left under `.agents/skills/`.
