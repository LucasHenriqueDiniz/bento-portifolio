# OPENCODE.md

OpenCode compatibility file.

The canonical instructions for this repository live in `CLAUDE.md`.
Follow the instructions there. Shared rules and skills now live in
`.claude/rules/` and `.claude/skills/` (referenced from `.opencode/settings.json`).
